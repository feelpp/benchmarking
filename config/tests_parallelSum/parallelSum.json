{
    "executable": "/data/home/cladellash/benchmarking/tests/data/parallelSum",
    "output_directory": "{{machine.output_app_dir}}/tests/parallelSum",
    "use_case_name": "parallel_sum",
    "timeout":"0-0:5:0",
    "platforms": {
        "builtin":{
            "input_dir":"",
            "append_app_option":[]
        }
    },
    "options": [
        "{{parameters.elements.value}}",
        "{{output_directory}}/outputs/{{instance}}"
    ],
    "outputs": [],
    "scalability": {
        "directory": "{{output_directory}}/outputs/{{instance}}/",
        "stages": [
            {
                "name":"",
                "file": "scalability.json",
                "format": "json",
                "variables_path":"*"
            }
        ]
    },
    "sanity": {
        "success": [],
        "error": []
    },
    "parameters": [
        {
            "name": "nb_tasks",
            "sequence": [
                {"tasks" : 1, "nodes":1, "exclusive_access":false},
                {"tasks" : 2, "nodes":1, "exclusive_access":false},
                {"tasks" : 4, "nodes":1, "exclusive_access":false},
                {"tasks" : 8, "nodes":1, "exclusive_access":false},
                {"tasks" : 16, "nodes":1, "exclusive_access":false}
            ]
        },
        {
            "name":"elements",
            "linspace":{
                "min":1000,
                "max":10000,
                "n_steps":5
            }
        }
    ]
}