{% macro render(plotNode,data,context={}) %}
{% set controller = FiguresController(data.get(plotNode.ref),plotNode.plot) %}
{% for figure in controller.figures %}

{%set _ = figure.writeCsvs(controller.data,context.attachments_dirpath) %}

{{figure.createTex(controller.data)}}

{% endfor %}
{% endmacro %}
